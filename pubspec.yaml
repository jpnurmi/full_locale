name: full_locale
description: egg # https://github.com/flutter/flutter/issues/92731
publish_to: "none"

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

dev_dependencies:
  flutter_lints: ^1.0.0

flutter:
  generate: true
  uses-material-design: true
